<%= form_with(model: document_page) do |form| %>
  <% if document_page.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(document_page.errors.count, "error") %> prohibited this document_page from being saved:</h2>

      <ul>
        <% document_page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :index, style: "display: block" %>
    <%= form.number_field :index %>
  </div>

  <div>
    <%= form.label :document_id, style: "display: block" %>
    <%= form.text_field :document_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
